/* eslint-disable jsx-a11y/accessible-emoji */
/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
import React from 'react'

const Example = (props) => {
    // switcheroos
    const a = useHook1(props)
    const b = useHook2(props)
    const c = useHook3(props)
    const d = useHook4(props)

    return (
        <p>Ola chicos 🌯</p>
    )
}

export default Example

    
